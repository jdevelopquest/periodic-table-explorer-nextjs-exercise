import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Periodic Table Explorer",
  description: "A web app for exploring the periodic table of elements",
};

export default function Home() {
  return <></>;
}
